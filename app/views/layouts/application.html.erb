<!DOCTYPE html>
<html>
  <head>
    <title>MusicPortfolio</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <!-- Swiper CSS -->
    <%= stylesheet_link_tag "https://unpkg.com/swiper/swiper-bundle.min.css", media: "all" %>

    <!-- Swiper JavaScript (Full Bundle) -->
    <%= javascript_include_tag "https://unpkg.com/swiper/swiper-bundle.min.js", defer: true %>




    <!-- Adobe Fonts -->
  <link rel="stylesheet" href="https://use.typekit.net/lnr4wfy.css">

    <!-- Font Awesome -->
  <link rel="stylesheet" href="path/to/font-awesome/css/font-awesome.min.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">

  <!-- ✅ GSAP JavaScript (Place before closing </head>) -->
    <script src="https://cdn.jsdelivr.net/npm/gsap@3.12.7/dist/gsap.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/gsap@3.12.7/dist/ScrollTrigger.min.js"></script>
  </head>

  <body>

<div class="page-container">
    <% unless controller_name == "home" && action_name == "index" %>
      <%= render "shared/navbar" %>
    <% end %>

      <div class="content-wrapper">
        <%= yield %> <!-- Main page content -->
      </div>

    <% unless controller_name == "home" && action_name == "index" %>
      <%= render "shared/footer" %>
    <% end %>
  </div>


</body>


</html>
